services:
  proxy:
    ports:
      - "${WANSIM_BIND_ADDRESS:-0.0.0.0}:${WANSIM_HTTPS_PORT:-443}:443"
    volumes:
      - ./nginx/https.conf:/etc/nginx/conf.d/default.conf:ro
      - ${WANSIM_TLS_DIR:?Set the directory containing fullchain.pem and privkey.pem}:/etc/nginx/certs:ro
